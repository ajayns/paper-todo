import React from 'react';

export const Filter = () => {
    return (
        <div>
            Show:
            <button>All</button>
            <button>Active</button>
            <button>Completed</button>
        </div>
    );
}

